@using Reef.Core.Models

<div class="space-y-4">
    <div>
        <label class="block text-sm font-medium text-gray-700 mb-1">Base Path *</label>
        <InputText @bind-Value="Config.BasePath"
                   class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-blue-500 focus:border-blue-500"
                   placeholder="C:/Apps/reef/exports or ./exports" />
        <p class="text-xs text-gray-500 mt-1">Absolute path (C:/data) or relative path (./exports, exports/data)</p>
    </div>

    <div class="flex items-center">
        <InputCheckbox @bind-Value="Config.UseRelativePath" id="use-relative-path"
                       class="h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 rounded" />
        <label for="use-relative-path" class="ml-2 block text-sm text-gray-700">
            Use Relative Path (relative to application directory)
        </label>
    </div>
</div>

@code {
    [Parameter]
    public DestinationConfiguration Config { get; set; } = new();

    [Parameter]
    public EventCallback<DestinationConfiguration> ConfigChanged { get; set; }
}
