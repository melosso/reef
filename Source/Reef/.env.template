# Reef Encryption Key (REQUIRED for production)
# Generate with: [Convert]::ToBase64String([System.Security.Cryptography.RandomNumberGenerator]::GetBytes(64))
REEF_ENCRYPTION_KEY=

# Environment
DOTNET_ENVIRONMENT=Development

# Override port (optional)
# REEF_PORT=8085